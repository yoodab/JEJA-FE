# JEJA 청년부 관리 시스템 전체 API 명세서

## 📋 목차

1. [인증 API](#1-인증-api)
2. [회원가입 신청 관리 API](#2-회원가입-신청-관리-api)
3. [멤버 관리 API](#3-멤버-관리-api)
4. [출석 관리 API](#4-출석-관리-api)
5. [일정 관리 API](#5-일정-관리-api)
6. [공지사항 API](#6-공지사항-api)
7. [앨범 API](#7-앨범-api)
8. [순 관리 API](#8-순-관리-api)
9. [새신자 관리 API](#9-새신자-관리-api)
10. [생일자 관리 API](#10-생일자-관리-api)
11. [장기결석자 관리 API](#11-장기결석자-관리-api)
12. [보고서 관리 API](#12-보고서-관리-api)
13. [재정 관리 API](#13-재정-관리-api)
14. [홈페이지 관리 API](#14-홈페이지-관리-api)
15. [팀/동아리 API](#15-팀동아리-api)
16. [내 정보 API](#16-내-정보-api)

---

## 1. 인증 API

### ✅ 1.1 로그인

- **엔드포인트**: `POST /api/auth/login`
- **권한**: 공용
- **요청 본문**:

```json
{
  "loginId": "user123",
  "password": "password123"
}
```

- **응답 형식**:
  - 헤더: `Authorization: Bearer {token}`
  - Body:

```json
{
  "status": "success",
  "code": "200",
  "message": "로그인 성공",
  "data": {
    "name": "홍길동",
    "role": "ADMIN"
  },
  "token": "jwt_token_here"
}
```

- **상태**: ✅ 구현됨

### ✅ 1.2 회원가입

- **엔드포인트**: `POST /api/auth/signup`
- **권한**: 공용
- **요청 본문**:

```json
{
  "loginId": "user123",
  "password": "password123",
  "name": "홍길동",
  "birthDate": "2000-01-01",
  "phone": "010-1234-5678"
}
```

- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "회원가입 성공"
}
```

- **상태**: ✅ 구현됨

---

## 2. 회원가입 신청 관리 API

### ✅ 2.1 승인 대기 사용자 목록 조회

- **엔드포인트**: `GET /api/admin/users/pending`
- **권한**: 관리자
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "승인 대기 사용자 목록 조회 성공",
  "data": [
    {
      "userId": 1,
      "loginId": "user123",
      "name": "홍길동",
      "phone": "010-1234-5678",
      "createdAt": "2024-01-15T10:30:00"
    }
  ]
}
```

- **상태**: ✅ 구현됨

### ✅ 2.2 사용자 승인

- **엔드포인트**: `PATCH /api/admin/users/{userId}/approve`
- **권한**: 관리자
- **경로 파라미터**: `userId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "사용자 승인 완료"
}
```

- **상태**: ✅ 구현됨

### ✅ 2.3 사용자 거절

- **엔드포인트**: `PATCH /api/admin/users/{userId}/reject`
- **권한**: 관리자
- **경로 파라미터**: `userId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "사용자 거절 완료"
}
```

- **상태**: ✅ 구현됨

### ✅ 2.4 승인된 사용자 목록 조회

- **엔드포인트**: `GET /api/admin/users/approved`
- **권한**: 관리자
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "승인된 사용자 목록 조회 성공",
  "data": [
    {
      "userId": 1,
      "loginId": "user123",
      "name": "홍길동",
      "phone": "010-1234-5678",
      "createdAt": "2024-01-15T10:30:00",
      "approvedAt": "2024-01-16T09:00:00"
    }
  ]
}
```

- **상태**: ✅ 구현됨

---

## 3. 멤버 관리 API

### ✅ 3.1 전체 멤버 목록 조회

- **엔드포인트**: `GET /api/admin/members`
- **권한**: 관리자
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "멤버 목록 조회 성공",
  "data": [
    {
      "memberId": 1,
      "name": "홍길동",
      "phone": "010-1234-5678",
      "birthDate": "2000-01-01",
      "status": "재적",
      "role": "리더",
      "soonId": 10,
      "soonName": "믿음셀",
      "hasAccount": true
    }
  ]
}
```

- **상태**: ✅ 구현됨

### ✅ 3.2 특정 멤버 상세 조회

- **엔드포인트**: `GET /api/admin/members/{memberId}`
- **권한**: 관리자
- **경로 파라미터**: `memberId` (number)
- **응답 형식**: 멤버 상세 정보 (3.1과 동일한 형식)
- **상태**: ✅ 구현됨

### ✅ 3.3 새 멤버 등록

- **엔드포인트**: `POST /api/admin/members`
- **권한**: 관리자
- **요청 본문**:

```json
{
  "name": "홍길동",
  "phone": "010-1234-5678",
  "birthDate": "2000-01-01",
  "status": "재적",
  "role": "일반"
}
```

- **응답 형식**: 생성된 `memberId` (number)
- **상태**: ✅ 구현됨

### ✅ 3.4 멤버 정보 수정

- **엔드포인트**: `PATCH /api/admin/members/{memberId}`
- **권한**: 관리자
- **경로 파라미터**: `memberId` (number)
- **요청 본문** (부분 업데이트 가능):

```json
{
  "name": "홍길동",
  "phone": "010-1234-5678",
  "birthDate": "2000-01-01",
  "status": "재적",
  "role": "리더"
}
```

- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "멤버 정보 수정 완료"
}
```

- **상태**: ✅ 구현됨

### ✅ 3.5 멤버 삭제

- **엔드포인트**: `DELETE /api/admin/members/{memberId}`
- **권한**: 관리자
- **경로 파라미터**: `memberId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "멤버 삭제 완료"
}
```

- **상태**: ✅ 구현됨

### ✅ 3.6 엑셀로 멤버 일괄 등록

- **엔드포인트**: `POST /api/admin/members/upload-excel`
- **권한**: 관리자
- **요청 형식**: `multipart/form-data`
- **요청 필드**: `file` (File)
- **응답 형식**: 등록된 멤버 수 (number)
- **상태**: ✅ 구현됨

---

## 4. 출석 관리 API

### ✅ 4.1 오늘 출석 여부 확인

- **엔드포인트**: `GET /api/attendance/today`
- **권한**: 로그인 사용자
- **응답 형식**:

```json
{
  "success": true,
  "message": "출석 상태 조회 성공",
  "data": {
    "hasAttended": true,
    "attendanceTime": "2024-12-15T11:00:00",
    "date": "2024-12-15"
  }
}
```

- **상태**: ✅ 구현됨

### ✅ 4.2 위치 기반 출석 체크

- **엔드포인트**: `POST /api/attendance/check`
- **권한**: 로그인 사용자
- **요청 본문**:

```json
{
  "date": "2024-12-15",
  "latitude": 37.5665,
  "longitude": 126.978
}
```

- **응답 형식**:

```json
{
  "success": true,
  "message": "출석 체크 완료",
  "data": {
    "attendanceId": 1,
    "date": "2024-12-15",
    "status": "PRESENT"
  }
}
```

- **상태**: ✅ 구현됨

### ❌ 4.3 특정 날짜 출석 목록 조회 (관리자)

- **엔드포인트**: `GET /api/admin/attendance/{date}`
- **권한**: 관리자
- **경로 파라미터**: `date` (YYYY-MM-DD)
- **쿼리 파라미터**:
  - `page` (number, 선택)
  - `size` (number, 선택)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "출석 목록 조회 성공",
  "data": {
    "date": "2024-12-15",
    "totalCount": 50,
    "presentCount": 45,
    "absentCount": 5,
    "attendanceList": [
      {
        "memberId": 1,
        "name": "홍길동",
        "status": "PRESENT",
        "attendanceTime": "2024-12-15T11:00:00",
        "latitude": 37.5665,
        "longitude": 126.978
      }
    ]
  }
}
```

- **상태**: ❌ 구현 필요

### ❌ 4.4 출석 데이터 저장 (관리자)

- **엔드포인트**: `POST /api/admin/attendance`
- **권한**: 관리자
- **요청 본문**:

```json
{
  "date": "2024-12-15",
  "attendanceData": {
    "1": true,
    "2": false,
    "3": true
  },
  "offering": 500000
}
```

- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "출석 데이터 저장 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 4.5 출석 통계 조회

- **엔드포인트**: `GET /api/admin/attendance/statistics`
- **권한**: 관리자
- **쿼리 파라미터**:
  - `startDate` (YYYY-MM-DD, 선택)
  - `endDate` (YYYY-MM-DD, 선택)
  - `memberId` (number, 선택)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "출석 통계 조회 성공",
  "data": {
    "totalDays": 30,
    "averageAttendance": 45,
    "attendanceRate": 0.9,
    "memberStatistics": [
      {
        "memberId": 1,
        "name": "홍길동",
        "attendanceCount": 27,
        "attendanceRate": 0.9
      }
    ]
  }
}
```

- **상태**: ❌ 구현 필요

---

## 5. 일정 관리 API

### ❌ 5.1 일정 목록 조회

- **엔드포인트**: `GET /api/schedules`
- **권한**: 로그인 사용자
- **쿼리 파라미터**:
  - `startDate` (YYYY-MM-DD, 선택)
  - `endDate` (YYYY-MM-DD, 선택)
  - `type` ('예배' | '행사' | '모임' | '기타', 선택)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "일정 목록 조회 성공",
  "data": [
    {
      "id": 1,
      "title": "주일예배",
      "date": "2024-12-15",
      "time": "11:00",
      "type": "예배",
      "location": "본당",
      "description": "청년부 주일예배"
    }
  ]
}
```

- **상태**: ❌ 구현 필요

### ❌ 5.2 일정 상세 조회

- **엔드포인트**: `GET /api/schedules/{scheduleId}`
- **권한**: 로그인 사용자
- **경로 파라미터**: `scheduleId` (number)
- **응답 형식**: 일정 상세 정보 (5.1과 동일한 형식)
- **상태**: ❌ 구현 필요

### ❌ 5.3 일정 추가 (관리자)

- **엔드포인트**: `POST /api/admin/schedules`
- **권한**: 관리자
- **요청 본문**:

```json
{
  "title": "주일예배",
  "date": "2024-12-15",
  "time": "11:00",
  "type": "예배",
  "location": "본당",
  "description": "청년부 주일예배"
}
```

- **응답 형식**: 생성된 `scheduleId` (number)
- **상태**: ❌ 구현 필요

### ❌ 5.4 일정 수정 (관리자)

- **엔드포인트**: `PATCH /api/admin/schedules/{scheduleId}`
- **권한**: 관리자
- **경로 파라미터**: `scheduleId` (number)
- **요청 본문**: 일정 추가와 동일 (부분 업데이트 가능)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "일정 수정 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 5.5 일정 삭제 (관리자)

- **엔드포인트**: `DELETE /api/admin/schedules/{scheduleId}`
- **권한**: 관리자
- **경로 파라미터**: `scheduleId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "일정 삭제 완료"
}
```

- **상태**: ❌ 구현 필요

---

## 6. 공지사항 API

### ❌ 6.1 공지사항 목록 조회

- **엔드포인트**: `GET /api/notices`
- **권한**: 로그인 사용자
- **쿼리 파라미터**:
  - `page` (number, 선택)
  - `size` (number, 선택)
  - `isImportant` (boolean, 선택)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "공지사항 목록 조회 성공",
  "data": {
    "totalCount": 50,
    "notices": [
      {
        "id": 1,
        "title": "[공지] 12월 청년부 모임 일정 안내",
        "content": "12월 청년부 모임은 매주 토요일 오후 7시에 진행됩니다.",
        "date": "2024-12-16",
        "isImportant": true,
        "createdBy": "관리자",
        "createdAt": "2024-12-16T10:00:00"
      }
    ]
  }
}
```

- **상태**: ❌ 구현 필요

### ❌ 6.2 공지사항 상세 조회

- **엔드포인트**: `GET /api/notices/{noticeId}`
- **권한**: 로그인 사용자
- **경로 파라미터**: `noticeId` (number)
- **응답 형식**: 공지사항 상세 정보
- **상태**: ❌ 구현 필요

### ❌ 6.3 공지사항 추가 (관리자)

- **엔드포인트**: `POST /api/admin/notices`
- **권한**: 관리자
- **요청 본문**:

```json
{
  "title": "[공지] 12월 청년부 모임 일정 안내",
  "content": "12월 청년부 모임은 매주 토요일 오후 7시에 진행됩니다.",
  "isImportant": true
}
```

- **응답 형식**: 생성된 `noticeId` (number)
- **상태**: ❌ 구현 필요

### ❌ 6.4 공지사항 수정 (관리자)

- **엔드포인트**: `PATCH /api/admin/notices/{noticeId}`
- **권한**: 관리자
- **경로 파라미터**: `noticeId` (number)
- **요청 본문**: 공지사항 추가와 동일 (부분 업데이트 가능)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "공지사항 수정 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 6.5 공지사항 삭제 (관리자)

- **엔드포인트**: `DELETE /api/admin/notices/{noticeId}`
- **권한**: 관리자
- **경로 파라미터**: `noticeId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "공지사항 삭제 완료"
}
```

- **상태**: ❌ 구현 필요

---

## 7. 앨범 API

### ❌ 7.1 앨범 목록 조회

- **엔드포인트**: `GET /api/albums`
- **권한**: 로그인 사용자
- **쿼리 파라미터**:
  - `page` (number, 선택)
  - `size` (number, 선택)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "앨범 목록 조회 성공",
  "data": [
    {
      "id": 1,
      "title": "2024 전도특공대",
      "date": "2024-03-16",
      "photoCount": 24,
      "thumbnail": "https://example.com/thumbnail.jpg"
    }
  ]
}
```

- **상태**: ❌ 구현 필요

### ❌ 7.2 앨범 상세 조회

- **엔드포인트**: `GET /api/albums/{albumId}`
- **권한**: 로그인 사용자
- **경로 파라미터**: `albumId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "앨범 상세 조회 성공",
  "data": {
    "id": 1,
    "title": "2024 전도특공대",
    "date": "2024-03-16",
    "photos": [
      {
        "id": 1,
        "url": "https://example.com/photo1.jpg",
        "description": "전도특공대 활동 사진"
      }
    ]
  }
}
```

- **상태**: ❌ 구현 필요

### ❌ 7.3 앨범 추가 (관리자)

- **엔드포인트**: `POST /api/admin/albums`
- **권한**: 관리자
- **요청 본문**:

```json
{
  "title": "2024 전도특공대",
  "date": "2024-03-16"
}
```

- **응답 형식**: 생성된 `albumId` (number)
- **상태**: ❌ 구현 필요

### ❌ 7.4 앨범에 사진 추가 (관리자)

- **엔드포인트**: `POST /api/admin/albums/{albumId}/photos`
- **권한**: 관리자
- **경로 파라미터**: `albumId` (number)
- **요청 형식**: `multipart/form-data`
- **요청 필드**: `file` (File), `description` (string, 선택)
- **응답 형식**: 생성된 `photoId` (number)
- **상태**: ❌ 구현 필요

### ❌ 7.5 앨범 삭제 (관리자)

- **엔드포인트**: `DELETE /api/admin/albums/{albumId}`
- **권한**: 관리자
- **경로 파라미터**: `albumId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "앨범 삭제 완료"
}
```

- **상태**: ❌ 구현 필요

---

## 8. 순 관리 API

### ❌ 8.1 순 목록 조회

- **엔드포인트**: `GET /api/admin/soons`
- **권한**: 관리자
- **쿼리 파라미터**:
  - `year` (number, 선택)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "순 목록 조회 성공",
  "data": [
    {
      "id": 1,
      "name": "믿음셀",
      "leader": "김리더",
      "leaderPhone": "010-1111-2222",
      "year": 2024,
      "members": [
        {
          "memberId": 1,
          "name": "홍길동",
          "role": "일반"
        }
      ]
    }
  ]
}
```

- **상태**: ❌ 구현 필요

### ❌ 8.2 순 생성 (관리자)

- **엔드포인트**: `POST /api/admin/soons`
- **권한**: 관리자
- **요청 본문**:

```json
{
  "name": "믿음셀",
  "leader": "김리더",
  "leaderPhone": "010-1111-2222",
  "year": 2024
}
```

- **응답 형식**: 생성된 `soonId` (number)
- **상태**: ❌ 구현 필요

### ❌ 8.3 순 수정 (관리자)

- **엔드포인트**: `PATCH /api/admin/soons/{soonId}`
- **권한**: 관리자
- **경로 파라미터**: `soonId` (number)
- **요청 본문**: 순 생성과 동일 (부분 업데이트 가능)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "순 수정 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 8.4 순에 멤버 배정 (관리자)

- **엔드포인트**: `POST /api/admin/soons/{soonId}/members`
- **권한**: 관리자
- **경로 파라미터**: `soonId` (number)
- **요청 본문**:

```json
{
  "memberIds": [1, 2, 3]
}
```

- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "멤버 배정 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 8.5 순에서 멤버 제거 (관리자)

- **엔드포인트**: `DELETE /api/admin/soons/{soonId}/members/{memberId}`
- **권한**: 관리자
- **경로 파라미터**:
  - `soonId` (number)
  - `memberId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "멤버 제거 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 8.6 순 삭제 (관리자)

- **엔드포인트**: `DELETE /api/admin/soons/{soonId}`
- **권한**: 관리자
- **경로 파라미터**: `soonId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "순 삭제 완료"
}
```

- **상태**: ❌ 구현 필요

---

## 9. 새신자 관리 API

### ❌ 9.1 새신자 목록 조회

- **엔드포인트**: `GET /api/admin/newcomers`
- **권한**: 관리자
- **쿼리 파라미터**:
  - `registered` ('Y' | 'N', 선택)
  - `assignedSoon` (string, 선택)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "새신자 목록 조회 성공",
  "data": [
    {
      "id": 1,
      "mdName": "조형진",
      "writeDate": "2024-12-01",
      "name": "김동환",
      "registered": "Y",
      "gender": "남성",
      "birthDate": "2000-05-15",
      "phone": "010-1234-5678",
      "assignedSoon": "믿음셀",
      "mentor": "김리더",
      "address": "서울시 강남구",
      "firstStatus": "친구와 함께 첫 방문",
      "middleStatus": "순모임 참석 시작",
      "recentStatus": "정착 진행 중",
      "soonAssignmentNote": "적극적인 성향, 빠른 순배치 권장"
    }
  ]
}
```

- **상태**: ❌ 구현 필요

### ❌ 9.2 새신자 추가 (관리자)

- **엔드포인트**: `POST /api/admin/newcomers`
- **권한**: 관리자
- **요청 본문**:

```json
{
  "mdName": "조형진",
  "writeDate": "2024-12-01",
  "name": "김동환",
  "registered": "N",
  "gender": "남성",
  "birthDate": "2000-05-15",
  "phone": "010-1234-5678",
  "assignedSoon": "",
  "mentor": "",
  "address": "서울시 강남구",
  "firstStatus": "친구와 함께 첫 방문",
  "middleStatus": "",
  "recentStatus": "",
  "soonAssignmentNote": ""
}
```

- **응답 형식**: 생성된 `newcomerId` (number)
- **상태**: ❌ 구현 필요

### ❌ 9.3 새신자 수정 (관리자)

- **엔드포인트**: `PATCH /api/admin/newcomers/{newcomerId}`
- **권한**: 관리자
- **경로 파라미터**: `newcomerId` (number)
- **요청 본문**: 새신자 추가와 동일 (부분 업데이트 가능)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "새신자 수정 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 9.4 새신자 삭제 (관리자)

- **엔드포인트**: `DELETE /api/admin/newcomers/{newcomerId}`
- **권한**: 관리자
- **경로 파라미터**: `newcomerId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "새신자 삭제 완료"
}
```

- **상태**: ❌ 구현 필요

---

## 10. 생일자 관리 API

### ✅ 10.1 생일자 목록 조회

- **엔드포인트**: `GET /api/admin/members` (멤버 목록에서 필터링)
- **권한**: 관리자
- **쿼리 파라미터**:
  - `month` (number, 1-12, 선택)
- **응답 형식**: 멤버 목록과 동일 (생일 기준 필터링)
- **상태**: ✅ 구현됨 (멤버 API 활용)

---

## 11. 장기결석자 관리 API

### ❌ 11.1 장기결석자 목록 조회

- **엔드포인트**: `GET /api/admin/absentees`
- **권한**: 관리자
- **쿼리 파라미터**:
  - `weeksAbsent` (number, 선택) - 최소 결석 주수
  - `careStatus` ('미연락' | '연락중' | '케어완료', 선택)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "장기결석자 목록 조회 성공",
  "data": [
    {
      "id": 1,
      "memberId": 5,
      "name": "최성민",
      "phone": "010-5678-9012",
      "lastAttendance": "2024-10-15",
      "weeksAbsent": 8,
      "careStatus": "미연락",
      "careNotes": "",
      "nextContactDate": null
    }
  ]
}
```

- **상태**: ❌ 구현 필요

### ❌ 11.2 장기결석자 케어 상태 수정 (관리자)

- **엔드포인트**: `PATCH /api/admin/absentees/{absenteeId}`
- **권한**: 관리자
- **경로 파라미터**: `absenteeId` (number)
- **요청 본문**:

```json
{
  "careStatus": "연락중",
  "careNotes": "전화 연락 완료, 다음 주 모임 참석 예정",
  "nextContactDate": "2024-12-20"
}
```

- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "케어 상태 수정 완료"
}
```

- **상태**: ❌ 구현 필요

---

## 12. 보고서 관리 API

### ❌ 12.1 보고서 템플릿 목록 조회

- **엔드포인트**: `GET /api/admin/reports/templates`
- **권한**: 관리자
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "보고서 템플릿 목록 조회 성공",
  "data": [
    {
      "id": 1,
      "name": "기본 보고서 템플릿",
      "description": "순모임 보고서 기본 템플릿",
      "fields": [
        {
          "id": "field1",
          "label": "출석 인원",
          "type": "number",
          "score": 1,
          "required": true
        }
      ],
      "createdAt": "2024-01-01T00:00:00",
      "createdBy": "관리자"
    }
  ]
}
```

- **상태**: ❌ 구현 필요

### ❌ 12.2 보고서 템플릿 생성 (관리자)

- **엔드포인트**: `POST /api/admin/reports/templates`
- **권한**: 관리자
- **요청 본문**:

```json
{
  "name": "기본 보고서 템플릿",
  "description": "순모임 보고서 기본 템플릿",
  "fields": [
    {
      "label": "출석 인원",
      "type": "number",
      "score": 1,
      "required": true
    }
  ]
}
```

- **응답 형식**: 생성된 `templateId` (number)
- **상태**: ❌ 구현 필요

### ❌ 12.3 보고서 템플릿 수정 (관리자)

- **엔드포인트**: `PATCH /api/admin/reports/templates/{templateId}`
- **권한**: 관리자
- **경로 파라미터**: `templateId` (number)
- **요청 본문**: 템플릿 생성과 동일 (부분 업데이트 가능)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "보고서 템플릿 수정 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 12.4 보고서 템플릿 삭제 (관리자)

- **엔드포인트**: `DELETE /api/admin/reports/templates/{templateId}`
- **권한**: 관리자
- **경로 파라미터**: `templateId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "보고서 템플릿 삭제 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 12.5 보고서 제출 목록 조회

- **엔드포인트**: `GET /api/admin/reports/submissions`
- **권한**: 관리자
- **쿼리 파라미터**:
  - `templateId` (number, 선택)
  - `soonName` (string, 선택)
  - `status` ('작성중' | '제출완료' | '승인완료', 선택)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "보고서 제출 목록 조회 성공",
  "data": [
    {
      "id": 1,
      "templateId": 1,
      "soonName": "믿음셀",
      "leaderName": "김리더",
      "date": "2024-12-15",
      "memberData": {
        "홍길동": {
          "field1": 1
        }
      },
      "totalScore": 10,
      "status": "제출완료"
    }
  ]
}
```

- **상태**: ❌ 구현 필요

### ❌ 12.6 보고서 제출 (관리자)

- **엔드포인트**: `POST /api/admin/reports/submissions`
- **권한**: 관리자
- **요청 본문**:

```json
{
  "templateId": 1,
  "soonName": "믿음셀",
  "leaderName": "김리더",
  "date": "2024-12-15",
  "memberData": {
    "홍길동": {
      "field1": 1
    }
  }
}
```

- **응답 형식**: 생성된 `submissionId` (number)
- **상태**: ❌ 구현 필요

### ❌ 12.7 보고서 승인 (관리자)

- **엔드포인트**: `PATCH /api/admin/reports/submissions/{submissionId}/approve`
- **권한**: 관리자
- **경로 파라미터**: `submissionId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "보고서 승인 완료"
}
```

- **상태**: ❌ 구현 필요

---

## 13. 재정 관리 API

### ❌ 13.1 재정 기록 목록 조회

- **엔드포인트**: `GET /api/admin/finance`
- **권한**: 관리자
- **쿼리 파라미터**:
  - `startDate` (YYYY-MM-DD, 선택)
  - `endDate` (YYYY-MM-DD, 선택)
  - `type` ('주일헌금' | '행사예산' | '정산', 선택)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "재정 기록 목록 조회 성공",
  "data": [
    {
      "id": 1,
      "date": "2024-12-14",
      "type": "주일헌금",
      "title": "12월 둘째주 주일헌금",
      "amount": 500000,
      "description": "청년부 주일헌금",
      "status": "완료"
    }
  ]
}
```

- **상태**: ❌ 구현 필요

### ❌ 13.2 재정 기록 추가 (관리자)

- **엔드포인트**: `POST /api/admin/finance`
- **권한**: 관리자
- **요청 본문**:

```json
{
  "date": "2024-12-14",
  "type": "주일헌금",
  "title": "12월 둘째주 주일헌금",
  "amount": 500000,
  "description": "청년부 주일헌금",
  "status": "완료"
}
```

- **응답 형식**: 생성된 `financeId` (number)
- **상태**: ❌ 구현 필요

### ❌ 13.3 재정 기록 수정 (관리자)

- **엔드포인트**: `PATCH /api/admin/finance/{financeId}`
- **권한**: 관리자
- **경로 파라미터**: `financeId` (number)
- **요청 본문**: 재정 기록 추가와 동일 (부분 업데이트 가능)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "재정 기록 수정 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 13.4 재정 기록 삭제 (관리자)

- **엔드포인트**: `DELETE /api/admin/finance/{financeId}`
- **권한**: 관리자
- **경로 파라미터**: `financeId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "재정 기록 삭제 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 13.5 재정 통계 조회

- **엔드포인트**: `GET /api/admin/finance/statistics`
- **권한**: 관리자
- **쿼리 파라미터**:
  - `startDate` (YYYY-MM-DD, 선택)
  - `endDate` (YYYY-MM-DD, 선택)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "재정 통계 조회 성공",
  "data": {
    "totalAmount": 5000000,
    "thisMonthAmount": 2000000,
    "byType": {
      "주일헌금": 3000000,
      "행사예산": 1500000,
      "정산": 500000
    }
  }
}
```

- **상태**: ❌ 구현 필요

---

## 14. 홈페이지 관리 API

### ❌ 14.1 슬라이드 목록 조회

- **엔드포인트**: `GET /api/admin/homepage/slides`
- **권한**: 관리자
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "슬라이드 목록 조회 성공",
  "data": [
    {
      "id": 1,
      "type": "text",
      "backgroundColor": "#1e293b",
      "textElements": [
        {
          "id": "text-1234567890",
          "text": "Welcome to JEJA Youth",
          "fontSize": 32,
          "color": "#ffffff",
          "x": 50,
          "y": 40,
          "fontWeight": "bold",
          "fontFamily": "Arial"
        }
      ],
      "title": null,
      "subtitle": null,
      "url": null,
      "linkUrl": null
    },
    {
      "id": 2,
      "type": "image",
      "url": "https://example.com/image.jpg",
      "linkUrl": "https://example.com",
      "title": "슬라이드 제목",
      "subtitle": "슬라이드 부제목",
      "backgroundColor": null,
      "textElements": null
    }
  ]
}
```

- **상태**: ❌ 구현 필요

### ❌ 14.2 슬라이드 추가

- **엔드포인트**: `POST /api/admin/homepage/slides`
- **권한**: 관리자
- **요청 본문** (텍스트 슬라이드):

```json
{
  "type": "text",
  "backgroundColor": "#1e293b",
  "textElements": [
    {
      "text": "Welcome to JEJA Youth",
      "fontSize": 32,
      "color": "#ffffff",
      "x": 50,
      "y": 40,
      "fontWeight": "bold",
      "fontFamily": "Arial"
    }
  ]
}
```

- **요청 본문** (이미지 슬라이드):

```json
{
  "type": "image",
  "url": "https://example.com/image.jpg",
  "linkUrl": "https://example.com",
  "title": "슬라이드 제목",
  "subtitle": "슬라이드 부제목"
}
```

- **응답 형식**: 생성된 `slideId` (number)
- **상태**: ❌ 구현 필요

### ❌ 14.3 슬라이드 수정

- **엔드포인트**: `PATCH /api/admin/homepage/slides/{slideId}`
- **권한**: 관리자
- **경로 파라미터**: `slideId` (number)
- **요청 본문**: 슬라이드 추가와 동일 (부분 업데이트 가능)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "슬라이드 수정 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 14.4 슬라이드 삭제

- **엔드포인트**: `DELETE /api/admin/homepage/slides/{slideId}`
- **권한**: 관리자
- **경로 파라미터**: `slideId` (number)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "슬라이드 삭제 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 14.5 슬라이드 순서 변경 (선택사항)

- **엔드포인트**: `PATCH /api/admin/homepage/slides/reorder`
- **권한**: 관리자
- **요청 본문**:

```json
{
  "slideIds": [3, 1, 2]
}
```

- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "슬라이드 순서 변경 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 14.6 유튜브 링크 조회

- **엔드포인트**: `GET /api/admin/homepage/youtube`
- **권한**: 관리자
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "유튜브 링크 조회 성공",
  "data": {
    "liveUrl": "https://www.youtube.com/channel/UCJekqH69c4VTieaH4N6ErsA/live",
    "playlistUrl": "https://www.youtube.com/embed/videoseries?list=PL-wQhvG4IAQRsNULw0nwgHKb-FOe-nFAu"
  }
}
```

- **상태**: ❌ 구현 필요

### ❌ 14.7 유튜브 링크 저장/수정

- **엔드포인트**: `PATCH /api/admin/homepage/youtube`
- **권한**: 관리자
- **요청 본문**:

```json
{
  "liveUrl": "https://www.youtube.com/channel/UCJekqH69c4VTieaH4N6ErsA/live",
  "playlistUrl": "https://www.youtube.com/embed/videoseries?list=PL-wQhvG4IAQRsNULw0nwgHKb-FOe-nFAu"
}
```

- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "유튜브 링크 저장 완료"
}
```

- **상태**: ❌ 구현 필요

### ❌ 14.8 공개 슬라이드 목록 조회 (일반 사용자용)

- **엔드포인트**: `GET /api/homepage/slides`
- **권한**: 로그인 사용자
- **응답 형식**: 관리자용과 동일 (읽기 전용)
- **상태**: ❌ 구현 필요

### ❌ 14.9 공개 유튜브 링크 조회 (일반 사용자용)

- **엔드포인트**: `GET /api/homepage/youtube`
- **권한**: 로그인 사용자
- **응답 형식**: 관리자용과 동일 (읽기 전용)
- **상태**: ❌ 구현 필요

---

## 15. 팀/동아리 API

### ❌ 15.1 팀 목록 조회

- **엔드포인트**: `GET /api/clubs`
- **권한**: 로그인 사용자
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "팀 목록 조회 성공",
  "data": [
    {
      "id": 1,
      "name": "찬양동아리",
      "leader": "박찬양",
      "meetingTime": "매주 금요일 오후 8시",
      "meetingPlace": "교회 3층 찬양실",
      "description": "함께 찬양하며 예배하는 팀입니다.",
      "memberCount": 12
    }
  ]
}
```

- **상태**: ❌ 구현 필요

### ❌ 15.2 내가 속한 팀 조회

- **엔드포인트**: `GET /api/clubs/my`
- **권한**: 로그인 사용자
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "내 팀 정보 조회 성공",
  "data": {
    "id": 1,
    "name": "찬양동아리",
    "leader": "박찬양",
    "meetingTime": "매주 금요일 오후 8시",
    "meetingPlace": "교회 3층 찬양실",
    "description": "함께 찬양하며 예배하는 팀입니다.",
    "members": [
      {
        "memberId": 1,
        "name": "박찬양",
        "role": "팀장"
      }
    ],
    "activities": [
      {
        "id": 1,
        "date": "2025-12-13",
        "title": "12월 정기 모임",
        "description": "새로운 찬양곡 연습 및 합주 연습"
      }
    ]
  }
}
```

- **상태**: ❌ 구현 필요

### ❌ 15.3 팀 생성 (관리자)

- **엔드포인트**: `POST /api/admin/clubs`
- **권한**: 관리자
- **요청 본문**:

```json
{
  "name": "찬양동아리",
  "leader": "박찬양",
  "meetingTime": "매주 금요일 오후 8시",
  "meetingPlace": "교회 3층 찬양실",
  "description": "함께 찬양하며 예배하는 팀입니다."
}
```

- **응답 형식**: 생성된 `clubId` (number)
- **상태**: ❌ 구현 필요

### ❌ 15.4 팀에 멤버 추가 (관리자)

- **엔드포인트**: `POST /api/admin/clubs/{clubId}/members`
- **권한**: 관리자
- **경로 파라미터**: `clubId` (number)
- **요청 본문**:

```json
{
  "memberIds": [1, 2, 3]
}
```

- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "멤버 추가 완료"
}
```

- **상태**: ❌ 구현 필요

---

## 16. 내 정보 API

### ❌ 16.1 내 정보 조회

- **엔드포인트**: `GET /api/users/me`
- **권한**: 로그인 사용자
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "내 정보 조회 성공",
  "data": {
    "userId": 1,
    "loginId": "youth123",
    "name": "김청년",
    "phone": "010-1234-5678",
    "birthDate": "1998-05-15",
    "role": "일반청년",
    "status": "재적",
    "soonId": 10,
    "soonName": "믿음셀",
    "hasAccount": true
  }
}
```

- **상태**: ❌ 구현 필요

### ❌ 16.2 내 출석 현황 조회

- **엔드포인트**: `GET /api/users/me/attendance`
- **권한**: 로그인 사용자
- **쿼리 파라미터**:
  - `startDate` (YYYY-MM-DD, 선택)
  - `endDate` (YYYY-MM-DD, 선택)
- **응답 형식**:

```json
{
  "status": "success",
  "code": "200",
  "message": "출석 현황 조회 성공",
  "data": {
    "thisMonth": 8,
    "lastMonth": 12,
    "thisYear": 45,
    "recentDates": ["2024-12-14", "2024-12-07", "2024-11-30"]
  }
}
```

- **상태**: ❌ 구현 필요

---

## 📊 API 구현 현황 요약

> **⚠️ 주의**: 이 문서는 프론트엔드 코드를 기반으로 작성되었습니다. 실제 백엔드 구현 상태는 백엔드 코드를 확인해야 정확히 알 수 있습니다.

### ✅ 프론트엔드에서 실제 API 호출 코드가 있는 것 (백엔드 구현 가능성 높음)

#### 1. 인증 API (2개) ✅

- `POST /api/auth/login` - 로그인
- `POST /api/auth/signup` - 회원가입
- **위치**: `src/services/authService.ts`

#### 2. 회원가입 신청 관리 API (4개) ✅

- `GET /api/admin/users/pending` - 승인 대기 사용자 목록 조회
- `PATCH /api/admin/users/{userId}/approve` - 사용자 승인
- `PATCH /api/admin/users/{userId}/reject` - 사용자 거절
- `GET /api/admin/users/approved` - 승인된 사용자 목록 조회
- **위치**: `src/services/adminService.ts`

#### 3. 멤버 관리 API (6개) ✅

- `GET /api/admin/members` - 전체 멤버 목록 조회
- `GET /api/admin/members/{memberId}` - 특정 멤버 상세 조회
- `POST /api/admin/members` - 새 멤버 등록
- `PATCH /api/admin/members/{memberId}` - 멤버 정보 수정
- `DELETE /api/admin/members/{memberId}` - 멤버 삭제
- `POST /api/admin/members/upload-excel` - 엑셀로 멤버 일괄 등록
- **위치**: `src/services/memberService.ts`, `src/services/adminService.ts`

#### 4. 출석 관리 API (2개) ✅

- `GET /api/attendance/today` - 오늘 출석 여부 확인
- `POST /api/attendance/check` - 위치 기반 출석 체크
- **위치**: `src/services/attendanceService.ts`

**총 14개 API** - 프론트엔드에서 실제 호출 코드가 있음

### ❌ 프론트엔드에서 Mock 데이터를 사용하는 것 (백엔드 미구현 가능성 높음)

#### 5. 일정 관리 API (5개) ❌

- **위치**: `src/pages/ScheduleListPage.tsx`, `ScheduleDetailPage.tsx`, `ScheduleManagePage.tsx`
- **상태**: Mock 데이터 사용 (`mockSchedules`)

#### 6. 공지사항 API (5개) ❌

- **위치**: `src/pages/YouthNoticePage.tsx`
- **상태**: Mock 데이터 사용 (`mockNotices`)

#### 7. 앨범 API (5개) ❌

- **위치**: `src/pages/YouthAlbumPage.tsx`
- **상태**: Mock 데이터 사용 (`mockAlbums`)

#### 8. 홈페이지 관리 API (9개) ❌

- **위치**: `src/pages/HomepageManagePage.tsx`
- **상태**: 로컬 스토리지 사용, TODO 주석 있음
- **코드 예시**: `// TODO: 실제 API 연동 시 구현`

#### 9. 순 관리 API (6개) ❌

- **위치**: `src/pages/SoonManagePage.tsx`
- **상태**: Mock 데이터 사용 (`initialSoons`, `allMembers`)

#### 10. 새신자 관리 API (4개) ❌

- **위치**: `src/pages/NewcomerManagePage.tsx`
- **상태**: Mock 데이터 사용 (`initialNewcomers`)

#### 11. 장기결석자 관리 API (2개) ❌

- **위치**: `src/pages/AbsenteeManagePage.tsx`
- **상태**: Mock 데이터 사용 (`initialAbsentees`)

#### 12. 보고서 관리 API (7개) ❌

- **위치**: `src/pages/ReportManagePage.tsx`
- **상태**: Mock 데이터 사용 (`initialTemplates`, `initialSubmissions`)

#### 13. 재정 관리 API (5개) ❌

- **위치**: `src/pages/FinanceManagePage.tsx`
- **상태**: Mock 데이터 사용 (`initialRecords`)

#### 14. 출석 관리 API (관리자용, 3개) ❌

- **위치**: `src/pages/AttendanceManagePage.tsx`
- **상태**: TODO 주석 있음 (`// TODO: 실제 API 호출로 변경 필요`)

#### 15. 내 정보 API (2개) ❌

- **위치**: `src/pages/MyInfoPage.tsx`
- **상태**: Mock 데이터 사용 (`mockMyInfo`, `mockAttendance`)

#### 16. 팀/동아리 API (4개) ❌

- **위치**: `src/pages/ClubPage.tsx`
- **상태**: Mock 데이터 사용 (`mockClubData`)

**총 약 60개 이상의 API** - 프론트엔드에서 Mock 데이터 사용 중

#### 우선순위 높음 (Phase 1) - 사용자 화면에서 바로 보이는 기능

1. **일정 관리 API** (5개) - `/schedules` 페이지
2. **공지사항 API** (5개) - `/youth-notices` 페이지
3. **앨범 API** (5개) - `/youth-album` 페이지
4. **홈페이지 관리 API** (9개) - `/homepage-manage` 페이지 (TODO 주석 있음)
5. **내 정보 API** (2개) - `/my-info` 페이지
6. **출석 관리 API (관리자용)** (3개) - `/manage/attendance` 페이지 (TODO 주석 있음)

#### 우선순위 중간 (Phase 2) - 관리자 전용 기능

7. **순 관리 API** (6개) - `/manage/soon` 페이지
8. **새신자 관리 API** (4개) - `/manage/newcomers` 페이지
9. **장기결석자 관리 API** (2개) - `/manage/absentees` 페이지
10. **재정 관리 API** (5개) - `/manage/finance` 페이지
11. **보고서 관리 API** (7개) - `/manage/reports` 페이지
12. **팀/동아리 API** (4개) - `/club` 페이지

---

## 🔐 공통 사항

### 인증

- 모든 관리자 API는 JWT 토큰이 필요합니다.
- 헤더에 `Authorization: Bearer {token}` 형식으로 전달해야 합니다.
- 일반 사용자 API도 로그인 토큰이 필요합니다.

### 공통 응답 형식

```json
{
  "status": "success" | "error",
  "code": "200" | "400" | "401" | "403" | "404" | "500",
  "message": "메시지",
  "data": { ... }
}
```

### 에러 응답

```json
{
  "status": "error",
  "code": "400",
  "message": "에러 메시지",
  "data": null
}
```

### 권한 에러

- 관리자 권한이 없는 경우: `403 Forbidden`
- 인증 토큰이 없는 경우: `401 Unauthorized`

---

## 📝 참고사항

1. **이 문서의 한계**:

   - 프론트엔드 코드만을 기반으로 작성되었습니다.
   - 실제 백엔드 구현 상태는 백엔드 코드를 확인해야 정확히 알 수 있습니다.
   - 프론트엔드에 API 호출 코드가 있다고 해서 백엔드가 완전히 구현되었다고 보장할 수 없습니다.

2. **확인 방법**:

   - ✅ **구현 가능성 높음**: `src/services/` 폴더에 실제 API 호출 함수가 있는 경우
   - ❌ **미구현 가능성 높음**: 페이지에서 `mock`, `임시`, `TODO` 주석이 있는 경우

3. **백엔드 연동 필요**:

   - 각 페이지의 TODO 주석을 확인하고 API를 활성화해야 합니다.
   - `HomepageManagePage.tsx`, `AttendanceManagePage.tsx` 등에 TODO 주석이 있습니다.

4. **데이터 저장**:

   - 현재는 로컬 스토리지에 임시 저장하고 있으나, 프로덕션에서는 백엔드에 저장해야 합니다.
   - `HomepageManagePage.tsx`에서 로컬 스토리지 사용 중

5. **이미지 업로드**:

   - 앨범 및 슬라이드 이미지 업로드는 `multipart/form-data` 형식을 사용해야 합니다.

6. **정확한 확인을 위해서는**:
   - 백엔드 코드 저장소 확인
   - 백엔드 API 문서 (Swagger 등) 확인
   - 실제 API 테스트
